RandomPythonApps
PrimeNumbers: Generates random number dumps them in a file, reads them back and mark them as prime
ReadDBandWriteTableAsCSV: Reads a databas (mdb/accdb) and writes a CSV for the specified table
